{
  "name": "vibi-showdown",
  "type": "module",
  "scripts": {
    "check": "tsc --target esnext --noEmit --skipLibCheck",
    "build:web": "bun build vibishowdown/index.ts --outdir vibishowdown/dist --target=browser --format=esm",
    "dev": "node scripts/serve-static.mjs",
    "server": "node -e \"console.error('Local server disabled. This project is locked to official VibiNet: wss://net.studiovibi.com'); process.exit(1)\""
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.13",
    "typescript": "^5.9.3"
  }
}
